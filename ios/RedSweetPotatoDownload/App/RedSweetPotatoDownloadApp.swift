import SwiftUI

@main
struct RedSweetPotatoDownloadApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
                .tint(Color(red: 0.29, green: 0.53, blue: 0.67))
        }
    }
}
