# 小红书抖音下载三端交付说明

## 交付范围

- Windows V2.6.0：便携桌面应用，支持小红书和抖音，默认系统下载目录，可自选目录。
- Android V1.3.0：原生 APK，支持两平台，默认 `Download/小红书抖音下载`，可用系统选择器授权其他目录。
- iPhone V0.3.0：iOS 12 兼容客户端，支持两平台，默认本机 Files 目录，可自选 Files/iCloud Drive 目录。
- 三端没有设备互联、云同步或账号体系，下载、设置和历史分别留在各自设备。

## 测试门槛

- 公共解析与存储单元测试全部通过。
- Windows 最终 EXE 必须启动、检查主题/设置完整性并做真实下载回归。
- Android 最终 release APK 必须在 Redmi K60 真机安装，测试主界面、设置、目录授权和真实下载。
- iPhone 必须通过 GitHub macOS/Xcode 的无签名模拟器编译；尚未使用 Apple 证书和 iPhone 真机验收，因此只发布源码预览，不声称可安装 IPA。

## 本次验收结果

验收记录按实际执行更新，不以编译代替真机或成品包测试。V2.6.0 Python 自动测试 35 项和 Ruff 检查通过；Windows 源码与最终便携 EXE 分别完成真实公开抖音 JPG 下载 1/1 回归，安装版可启动并正常响应，排除无关模块后便携包由约 75 MB 降至约 61 MB。Android 1.3.0 的 5 项 JUnit 测试通过，lint vital、release APK 构建和 aapt 身份检查通过；项目已补 Gradle Wrapper，但本机首次下载固定 Gradle 分发包时网络超时，因此本轮使用已缓存 Gradle 完成构建。iPhone 0.3.0 已通过 GitHub Actions `30528141205` 的模拟器、iPhoneOS Release、IPA 结构与版本校验。交付时 ADB 与 USB 均未识别手机，所以 Android / iPhone 真机安装与下载仍不得标记通过。

## 数据与输出

每条笔记目录只包含原始媒体和 `文案.txt`。文案包含标题、正文、话题。文件夹名为 `评数字-赞数字-标题-作者`。历史 JSON 只集中保存下载网址、笔记 ID 和标题，不散落到各笔记目录。

## 分发与恢复

GitHub 源码、提交、标签和 Release 是云端备份。上一稳定版保留不覆盖。共享 AI 技能及开发手册同步更新，便于 Codex、Agents 和后续开发继续调用和复用。
