interface:
  display_name: "小红书下载（兼容入口）"
  short_description: "旧调用名，自动转交万能下载器"
  default_prompt: "Use $xhs-download to route this legacy download request to $universal-downloader."
policy:
  allow_implicit_invocation: false
