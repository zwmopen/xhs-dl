# xhs-dl 项目交接文档

## 2026-08-11 当前交接胶囊

- 2026-08-13 / Windows V2.7.0：在同一 `UnifiedDownloader` 增加 `generic` 路由和本地 `YtDlpEngine`，首批识别 X/Twitter、B站、YouTube、TikTok、Instagram、Facebook、Pinterest、Reddit、Vimeo、Bluesky；小红书、抖音原专用引擎不变。通用输出继续使用统一目录、媒体名、`文案.txt` 和集中历史。
- V2.7.0 安全边界：不读取浏览器 Cookie，不接公共 Cobalt/第三方解析接口；平台强制登录或机器人确认时直接报告。yt-dlp 与 imageio-ffmpeg 作为随包本地依赖。
- V2.7.0 实测：X `TopHeroes_` 公开帖子下载 3.4 MB MP4 成功；B站 `BV13x41117TL` 下载并合并 116.6 MB MP4 成功，文件头均为有效 `ftyp`；YouTube 测试链接触发匿名机器人确认，未绕过。

- 正式产品名：**小红书抖音下载**。内部仓库名仍为 `xhs-dl`，Android applicationId 与 iOS Bundle ID 保持不变，避免覆盖其他 App。
- 唯一真实产品仓库是 `D:\AICode\工具开发\projects\xhs-dl`（远端 `zwmopen/xhs-dl`）；`D:\AICode\工具开发\projects\XHS_Downloader` 是独立第三方解析引擎，`D:\AICode\运行数据\迁移暂存\重复仓库\xhs-dl-ai-repos-20260721` 只是历史重复仓库归档，禁止从归档恢复成平行项目。
- Windows V2.6.2、Android V1.3.1、iPhone V0.3.1 三套独立客户端均已接入小红书与抖音公开作品；不做云同步，也不共享同一运行时下载核心。
- 抖音技术路线：Windows 用 Playwright 启动不带用户资料的临时系统 Edge；Android 用应用内 WebView；iPhone 用 `WKWebsiteDataStore.nonPersistent()`。普通公开作品不读取 Cookie 或浏览器登录态。
- 输出：文件夹 `评评论-赞点赞-标题-作者`；默认图片为原尺寸、质量 95 的 JPG，可在设置中改 PNG 或保持原格式；文件名为 `封面-标题`、`内页N-标题`、`视频-标题`；同目录只放媒体与 `文案.txt`。
- 历史：集中 `history.json`，每条只保留下载网址、笔记/作品 ID、标题，不在作品目录散落 JSON。
- 设置与视觉：三端统一拟态悬浮 / 克制玻璃语言；设置包含目录、频率、图片格式、更新检测、使用说明、设计理念和安全边界；小屏必须可滚动且保存按钮可达。
- V2.6.0 深度审计修复：桌面后台异常回调失效、混合媒体编号、多视频覆盖、移动端更新检测失效、抖音大文件内存占用/半文件、错误页误存媒体、损坏设置启动风险，以及 Web 备用端只路由小红书的问题。
- V2.6.1 现场根因：本机 `no_proxy` 含裸 IPv6 `::1` / `::1/128`，上游 `httpx 0.28` 在创建客户端时误解析为非法端口 `:1`，使旧桌面入口的小红书任务全部失败。修复只在启动上游子进程前过滤无法兼容的裸 IPv6 bypass 项，保留现有代理和普通 `NO_PROXY` 规则；同时隐藏内部空元数据标记并保留可理解错误。
- V2.6.1 真实回归：公开小红书单图 1/1、多图 6/6、视频 1/1、Emoji 标题多图 8/8；JPG 全部可解码，MP4 含有效 `ftyp`，中文/Emoji 文件名可读，作品目录只有媒体与 `文案.txt`。Python 37 项、Ruff 与 compileall 通过。
- V2.6.1 最终便携 EXE 在全新目录完成真实单图下载 1/1；正式安装入口为 `C:\Users\z\AppData\Local\Programs\XHS下载器\小红书抖音下载.exe`，桌面唯一正式快捷方式为 `C:\Users\z\Desktop\XHS下载器.lnk`。旧 `红薯下载.lnk` 已移到 `D:\AICode\备份\XHS下载器\桌面快捷方式\红薯下载-v2.4.0.lnk`，没有删除旧程序；快捷方式实点后窗口标题正确且进程响应正常。
- V2.6.2 现场根因：小红书分享域名新增 `xhslink.cn`，产品平台路由和独立上游引擎仍只列出 `xhslink.com`，导致先报“不支持”，补路由后又在上游链接提取阶段失败。产品适配器现在把 `.cn` 短链规范化为同分享码的 `.com` 等价入口再交给旧上游，集中历史仍记录用户原始 `.cn` URL；同时修复 Skill 在 GBK 控制台输出 🍃 等 Emoji 时下载已成功却因 JSON 汇报报错的问题。
- V2.6.2 真实回归：`http://xhslink.cn/o/97Pz4siAYx4` 下载 15/15 张 JPG、完整标题/正文/话题文案，文件夹 `评3-赞2-山野露营农庄小院🍃杭州团建包院天花板-陀螺团建日记`，Skill 最终返回 `success 1 / failed 0`。
- V2.6.2 正式包回归：Windows 便携包重新构建并替换稳定安装目录，安装后 EXE 在独立目录再次下载同一 `.cn` 链接成功；Python 40 项、Ruff、compileall 通过。Android 1.3.1 路由单测、lint vital、Release APK 通过；iOS 0.3.1 Actions `31474284287` 在提交 `98c0263` 上完成模拟器、未签名真机构建、IPA 校验与上传。移动端仍没有连接设备，不把构建通过写成真机通过。
- 2026-08-10 移动端现场：Android 1.3.0 的 5 项 JUnit、lint vital、Release APK、包名/版本/SDK 和 v2 签名通过；iPhone 0.3.0 在当前主分支重新通过 CI `31392173231`，模拟器、iPhoneOS Release、IPA 结构与上传均成功。当前 `adb devices` 为空且 Windows 未识别 iPhone/Android USB 设备，因此不得声称本轮真机安装、启动或下载通过。
- 本地验证：Python 35 项与 Ruff 静态检查通过；源码和最终便携 EXE 分别完成真实公开抖音图文 1/1，均生成 JPG 与文案；安装版启动正常。Android 5 项 JUnit、lint vital、release APK 和 aapt 身份检查通过；iOS GitHub Actions `30528141205` 完成模拟器、iPhoneOS Release、IPA 结构与版本校验。
- 构建复现：已补 Gradle 8.11.1 Wrapper；本机首次下载分发包受网络连接超时影响，故本轮 Android 构建使用已缓存 Gradle 9.4.1，单元测试通过手动 JUnitCore 执行。CI/其他网络正常环境应优先使用 `android/gradlew`。
- 仍待真机：当前 ADB 与 USB 均未识别手机，因此 Android / iPhone 本轮真机抖音下载与安装不得标记通过；设备恢复后按 `ios/IOS-ACCEPTANCE.md` 和 Android 验收清单补测。
- V2.6.0 功能提交为 `1ad406a`，标签 `v2.6.0` 指向验收文档提交 `edae1ea`；正式 Release 已发布到 `https://github.com/zwmopen/xhs-dl/releases/tag/v2.6.0`，包含 Windows、Android、未签名 iOS、源码、共享技能和 SHA256。真机状态仍保持待验收。
- 共享技能：`D:\AICode\AI\skills\技能包\技能\xhs-download` 已改为免登录双平台统一引擎；V2.6.0 同步修复自检项目发现、默认目录及自检日志污染，技能仓库提交 `ca68486`，普通下载不使用 Cookie 采集路线。

> 以下为 2026-07-18 的 V1 历史档案，仅供理解早期路线；不得覆盖上方当前交接胶囊。

---

## 一、项目概述

**xhs-dl** 是一个小红书笔记下载器，支持从 xhslink 短链接或分享文本中提取笔记正文和图片，保存到本地。无需登录账号。

- **CLI 版**: 命令行工具 `xhs-dl`
- **Web 版**: 浏览器界面 `xhs-dl-web`，粘贴链接点下载
- **Python SDK**: 可被其他项目 import 调用

### 已知限制（重要）

1. **图片带水印**: 小红书服务端在存储图片时就烧录了水印（半透明"小红书"字样），技术上无法通过修改 URL 去除。这不是代码 bug，是平台行为。
2. **视频笔记**: V1 仅下载封面图，不下载视频文件。
3. **风控**: 频繁请求会被临时拦截（返回安全页面/404），工具已内置 2-5 秒随机延迟。

---

## 二、技术原理

```
用户输入 xhslink.com 短链接
    ↓ requests.get 跟踪 302 重定向
完整 URL（含 xsec_token、app_platform 等全部参数）
    ↓ 用同一 session（模拟 iPhone Safari UA）GET 该 URL
HTML 页面（服务端 SSR 渲染，约 150KB）
    ↓ 正则提取 window.__INITIAL_STATE__ = {...}
JSON 数据（括号计数法确保完整匹配）
    ↓ 从 JSON 中提取笔记数据
路径1: state.noteData.data.noteData（新版，最常见）
路径2: state.note.noteDetailMap[noteId].note（旧版）
路径3: state.noteData.normalNotePreloadData（预加载，可能不完整）
    ↓ 提取图片 URL（imageList / infoList）
图片 URL 特征: https://sns-webpic-qc.xhscdn.com/.../notes_pre_post/...!h5_1080jpg
    ↓ HTTP→HTTPS + 带 Referer 头下载
本地文件
```

**关键发现**:
- 必须用短链接重定向后的**完整 URL**（带 xsec_token），直接用 note_id 访问 /explore/ 会被安全系统拦截到 404 页面
- 图片 URL 末尾的 `!h5_1080jpg` 是 CDN 认证后缀，**不能去掉**，去掉会 403
- 图片 URL 从 HTTP 换成 HTTPS 即可正常下载
- User-Agent 用 iPhone Safari 效果最好

---

## 三、项目结构

```
xhs-dl/
├── xhs_dl/
│   ├── __init__.py              # 包入口，导出 XhsDownloader, extract_urls_from_text
│   ├── cli.py                   # CLI 入口，argparse 解析
│   ├── core/
│   │   ├── __init__.py
│   │   ├── downloader.py        # 核心下载逻辑（XhsDownloader 类）
│   │   └── models.py            # 数据模型（NoteResult, DownloadResult）
│   └── web/
│       ├── __init__.py
│       └── app.py               # Web 界面（内置 HTML，单文件，端口 5678）
├── tests/
│   └── test_basic.py            # 4 个单元测试，已全部通过
├── pyproject.toml               # 包配置，定义了 xhs-dl 和 xhs-dl-web 两个入口命令
├── requirements.txt             # 唯一依赖: requests>=2.28.0
├── LICENSE                      # MIT
├── README.md                    # 使用文档
└── .gitignore
```

---

## 四、核心代码说明

### 4.1 downloader.py（核心）

**XhsDownloader 类**:

```python
dl = XhsDownloader(
    output_dir="./xhs_downloads",  # 保存目录
    delay=(2, 5),                   # 请求间随机延迟
    on_progress=callback_fn,       # 进度回调
)

# 方式1: 传入链接列表
result = dl.download(["http://xhslink.com/o/xxx", ...])

# 方式2: 传入分享文本（自动提取链接）
result = dl.download_text("快存下！vivo X300 http://xhslink.com/o/xxx")
```

**关键方法**:
| 方法 | 作用 |
|------|------|
| `_resolve_url(url)` | 跟踪短链接 302 重定向，返回完整 URL |
| `_parse_note_id(url)` | 从 URL 正则提取 24 位 hex 的 note_id |
| `_extract_ssr_state(html)` | 括号计数法从 HTML 提取 `__INITIAL_STATE__` JSON |
| `_extract_note(state)` | 从 JSON 中按 3 条路径查找笔记数据 |
| `_get_media_urls(note)` | 从 imageList 提取图片 URL，兼容 infoList/info_list |
| `_download_file(url, path)` | 带 Referer 头下载，验证文件 >200B |
| `_sanitize(name)` | 清理文件名非法字符 |

**SSR 数据结构（实测）**:
```
window.__INITIAL_STATE__ = {
  "noteData": {
    "data": {
      "noteData": {           // <-- 主要数据在这里
        "title": "...",
        "desc": "...",
        "type": "normal",     // "normal" | "video"
        "user": {"nickname": "..."},
        "imageList": [
          {
            "url": "http://sns-webpic-qc.xhscdn.com/.../!h5_1080jpg",
            "url_default": "",   // SSR 中通常为空
            "infoList": [],       // SSR 中通常为空
            "fileId": "...",
            "width": 1080,
            "height": 1440,
          }
        ],
        "video": {...}        // 视频笔记才有
      }
    },
    "normalNotePreloadData": {  // 预加载（备用）
      "title": "...",
      "desc": "...",
      "imagesList": [{"url": "...", "width": ..., "height": ...}]
    }
  }
}
```

### 4.2 cli.py

argparse 定义，支持:
- 位置参数 `urls`: 链接或分享文本
- `-f/--file`: 从文件读取
- `-o/--output`: 保存目录
- 自动从文本中正则提取 URL

### 4.3 web/app.py

- 纯 Python 标准库 `http.server`，无额外依赖
- 内嵌 HTML/CSS/JS（小红书主题色 #ff2442）
- `GET /` → 返回页面
- `POST /api/download` → 接收 `{text, output_dir}`，同步下载后返回 JSON
- 端口 5678，启动后自动打开浏览器

---

## 五、测试结果

2026-07-18 实测 6 个链接，全部成功:

| 笔记 | 图片数 | 正文 | 图片下载 |
|------|--------|------|----------|
| vivo X300隐藏功能 | 6 | OK | 6/6 |
| 红米 Watch 6 隐藏功能 | 6 | OK | 6/6 |
| 小米15 Ultra隐藏功能 | 6 | OK | 6/6 |
| AirPods Pro 隐藏功能 | 5 | OK | 5/5 |
| Mac 12个设置 | 6 | OK | 6/6 |
| Apple Watch 24项设置 | 8 | OK | 8/8 |

单元测试 4/4 通过: extract_urls, parse_note_id, sanitize, extract_ssr_state

---

## 六、待改进 / V2 方向

1. **视频下载**: SSR 中的 video 字段包含视频信息，但视频 URL 可能需要额外处理（consumer.originVideoKey 等字段）
2. **去水印研究**: 当前 CDN 返回的图片确实带水印。可能的探索方向:
   - 检查是否有其他图片尺寸/格式后缀（如原始图）能绕过水印
   - 研究 App 端 API 是否返回无水印图
3. **并发下载**: 当前是串行下载，可以加线程池
4. **进度显示**: Web 版目前是同步阻塞的，大量链接时浏览器会卡住等待，应该改成异步（SSE 或 WebSocket 推送进度）
5. **GUI 版本**: 用 PyQt/Tkinter 做桌面应用
6. **错误重试**: 加入自动重试机制
7. **Cookie/登录**: 可选支持登录后获取更多数据（如私密笔记）
8. **Docker 化**: 方便部署
9. **图片格式检测**: 根据 URL 后缀或 Content-Type 保存为正确格式（jpg/png/webp）

---

## 七、使用方法（给下一个人的快速上手）

```bash
# 安装
cd xhs-dl
pip install -r requirements.txt

# CLI
python -m xhs_dl.cli "http://xhslink.com/o/xxxxx"
python -m xhs_dl.cli -f links.txt -o ./output

# Web
python -m xhs_dl.web.app
# 打开 http://127.0.0.1:5678

# Python 调用
from xhs_dl import XhsDownloader
dl = XhsDownloader(output_dir="./notes")
result = dl.download(["http://xhslink.com/o/xxxxx"])
```

---

## 八、环境要求

- Python >= 3.9
- requests >= 2.28.0
- 无其他依赖
- 需要能访问 xiaohongshu.com 和 xhscdn.com 的网络环境
